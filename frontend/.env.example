# Base path the browser calls. Vite proxies this to the Flask backend in dev.
VITE_API_BASE_URL=/api

# Flask backend address used by the Vite dev-server proxy.
VITE_DEV_API_TARGET=http://localhost:8000

# Request timeout in milliseconds.
VITE_API_TIMEOUT=20000
