"""Automations for גאוסיין.

Tests live in ``tests/``. Page objects and locators are imported from ``core``,
so every application drives the browser the same way; this folder holds only what
is specific to גאוסיין.
"""
